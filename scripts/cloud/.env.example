# Cloud Execution Configuration
# Copy to .env and customize:
#   cp .env.example .env

# Cloud Provider: codespaces (推奨) | ec2
CLOUD_PROVIDER=codespaces

# --- Codespaces (デフォルト) ---
# basicLinux32gb: 2 vCPU / 8GB RAM ($0.18/hr)
# standardLinux32gb: 4 vCPU / 16GB RAM ($0.36/hr)
# largePremiumLinux: 8 vCPU / 32GB RAM ($0.72/hr) ※プランにより利用不可の場合あり
CODESPACE_MACHINE=basicLinux32gb
CODESPACE_IDLE_TIMEOUT=30          # アイドルタイムアウト（分）
CODESPACE_RETENTION_DAYS=7         # 停止後の保持日数

# --- EC2 (EC2使用時のみ設定) ---
# CLOUD_PROVIDER=ec2 に変更した場合のみ以下を設定
#EC2_HOST=                          # EC2のホスト名またはIP
#EC2_USER=ec2-user                  # SSHユーザー名
#EC2_KEY=                           # SSH秘密鍵パス（未設定なら -i なし）
#EC2_WORK_DIR=/home/ec2-user/work   # 作業ディレクトリ
#EC2_LOG_DIR=/home/ec2-user/logs    # ログディレクトリ
#EC2_INSTANCE_ID=                   # EC2インスタンスID（start/stop/statusに必要）
#EC2_REGION=ap-northeast-1          # AWSリージョン
#AWS_PROFILE=                       # AWSプロファイル（未設定ならデフォルト）

# --- Billing Alert (EC2 only) ---
BILLING_ALERT_EMAIL=               # 通知先メールアドレス
BILLING_THRESHOLD=100              # 月額閾値（USD）
