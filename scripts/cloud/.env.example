# Cloud Execution Configuration
# Copy to .env and fill in your values:
#   cp .env.example .env

# --- SSH Connection ---
CLOUD_HOST=           # EC2のIP or ~/.ssh/config のHost名
CLOUD_USER=ec2-user   # SSHユーザー
CLOUD_KEY=            # SSHキーパス（例: ~/.ssh/my-key.pem）。~/.ssh/config 利用時は空でOK

# --- Remote Paths ---
CLOUD_WORK_DIR=/home/ec2-user/work # リモート作業ディレクトリ（絶対パスで指定）
CLOUD_LOG_DIR=/home/ec2-user/logs  # リモートログディレクトリ（絶対パスで指定）

# --- AWS Settings ---
AWS_REGION=ap-northeast-1
AWS_PROFILE=          # AWS CLIプロファイル名（orch up/down用。未設定ならdefault）
EC2_INSTANCE_TYPE=t3.2xlarge
EC2_AMI=              # Amazon Linux 2023 AMI ID
EC2_KEY_NAME=         # EC2キーペア名
EC2_SECURITY_GROUP=   # セキュリティグループID
EC2_SUBNET=           # サブネットID
EC2_INSTANCE_ID=      # セットアップ後に記入

# --- Options ---
SSH_OPTIONS=-oLogLevel=ERROR  # SSH警告抑制（推奨）。複数: -oLogLevel=ERROR -oStrictHostKeyChecking=no

# --- CloudWatch Billing Alert ---
BILLING_ALERT_EMAIL=  # 課金アラート通知先メールアドレス
BILLING_THRESHOLD=100 # 月額課金アラート閾値（USD）
